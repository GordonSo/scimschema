# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py35, py36, py37, py38

[testenv]
deps = pytest, pytest-xdist       # install pytest in the virtualenv where commands will be executed
commands =
    # whatever extra steps before testing might be necessary
    pytest  -n auto        # or any other test runner that you might use
